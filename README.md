# Workout tracker

Web app for tracking workouts using React and Express

### Run locally:

Start the server:

```sh
yarn start
```

Start the client (in client directory):

```sh
cd client
yarn start
```
